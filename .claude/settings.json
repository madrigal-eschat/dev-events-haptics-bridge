{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); echo \"$cmd\" | grep -qE '^git commit' && (cargo fmt --check && cargo check && cargo test) || true",
            "timeout": 120,
            "statusMessage": "Running cargo fmt --check, check, test..."
          }
        ]
      }
    ]
  }
}
